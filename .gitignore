# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# n8n exports may include credential blobs if exported with credentials — never commit those
*.credentials.json
**/credentials/*.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
Thumbs.db

# Node (in case any tooling lands here)
node_modules/
npm-debug.log
yarn-error.log
